# P0-4 密码明文存储 → BCrypt 加密存储

> 分支：`feature/p0-4-password-bcrypt`
> 完成日期：2026-09-19
> 对应改进清单：P0-4

---

## 一、改之前是什么问题

### 问题 1：数据库里存的是明文

```
mysql> select id, username, password, name from emp order by id limit 5;
+----+-------------+----------+--------+
| id | username    | password | name   |
+----+-------------+----------+--------+
|  1 | shinaian    | 123456   | 施耐庵 |
|  2 | songjiang   | 123456   | 宋江   |
|  3 | lujunyi     | 123456   | 卢俊义 |
...
```

全库 30 个账号，密码字段就是人眼可读的明文。三种不同的密码：

| 明文 | 账号数 |
|---|---|
| `123456` | 28 |
| `1234567` | 1 |
| `12345678` | 1 |

**危害有多大**：数据库一旦泄漏（SQL 注入、备份文件被拿走、运维误操作导出、离职员工拷走），攻击者**不需要做任何破解**，直接就看到所有人的密码。而且用户往往在别处复用同一个密码，泄漏的不只是这一个系统。

还有一层：`123456` 这种密码在彩虹表里是"零秒命中"。就算加了密，没有盐的哈希（MD5/SHA1）也一样能被瞬间反查出来 —— 这也是后面选 BCrypt 而不是 MD5 的原因。

### 问题 2：接口把密码明文返回给前端 —— 实测确认

这一条是改的过程中顺带挖出来的，比第 1 条更直接。

[`EmpMapper.xml`](tlias-web-management/src/main/resources/com/itheima/mapper/EmpMapper.xml) 的分页查询写的是 `select emp.*`，会把 `password` 列一起查出来；而 [`Emp.java`](tlias-web-management/src/main/java/com/itheima/pojo/Emp.java) 的 `password` 字段没有任何序列化排除，于是这个字段被原样塞进 JSON 返回。

**改动前的实测结果**（我用 `git stash` 临时撤掉修复、重新编译启动后抓的）：

```json
GET /emps?page=1&pageSize=1     ← 只要登录过就能调
{
  "id": 1,
  "username": "shinaian",
  "password": "123456",          ← 明文密码
  "name": "施耐庵",
  ...
}
```

改之前 `GET /emps?page=1&pageSize=10` 的响应里 `password` 出现 **10 次**（每行一次）。也就是说：**任何一个能登录的普通员工，调一下员工列表接口，就能拿到全公司所有人的密码。**

这不是"数据库被攻破才有风险"，而是"打开浏览器就能看到"。

### 问题 3：日志里全是明文密码

两处：

**(a) 登录日志** —— [`LoginController.java`](tlias-web-management/src/main/java/com/itheima/controller/LoginController.java)：

```java
log.info("员工登录:{}", emp);   // emp 是 Lombok @Data 生成的 toString, 含 password
```

**每次登录都会往日志文件里写一条明文密码。**

**(b) 操作日志** —— [`OperationLogAspect`](tlias-web-management/src/main/java/com/itheima/aspect/OperationLogAspect.java) 用 `Arrays.toString(args)` 记录方法参数，走的就是 Lombok 的 `toString()`，同样带密码。这个会**落库**到 `operate_log.method_params`，历史记录里的密码是长期留存的。

数据库里的旧记录能看到：

```
method_params: [Emp(id=52, username=..., password=, name=老邓, ...)]
                                               ^^^^^^^^^^^^
```

### 问题 4：数据库列宽根本装不下 BCrypt 密文

`emp.password` 原本是 **`varchar(50) DEFAULT '123456'`**。

而 BCrypt 密文固定 **60 个字符**。实测：

```
mysql> insert into _t_col_test(password) values ('$2a$10$4Gf9H5hkfr2rGnooSdA8F.r/...');
ERROR 1406 (22001): Data too long for column 'password' at row 1
```

**如果只改代码不改表结构，整个功能会在插入时直接炸掉。** 这是必须先处理的前置条件。

---

## 二、改动清单

| 文件 | 类型 | 说明 |
|---|---|---|
| [`pom.xml`](tlias-web-management/pom.xml) | 修改 | 引入 `spring-security-crypto`（只引 crypto 模块） |
| [`config/PasswordConfig.java`](tlias-web-management/src/main/java/com/itheima/config/PasswordConfig.java) | 新增 | `PasswordEncoder` Bean（BCrypt） |
| [`pojo/Emp.java`](tlias-web-management/src/main/java/com/itheima/pojo/Emp.java) | 修改 | 密码字段禁止序列化输出 + 不进 toString |
| [`mapper/EmpMapper.java`](tlias-web-management/src/main/java/com/itheima/mapper/EmpMapper.java) | 修改 | 登录改按用户名查；insert 补上 password 列 |
| [`service/impl/EmpServiceImpl.java`](tlias-web-management/src/main/java/com/itheima/service/impl/EmpServiceImpl.java) | 修改 | 登录用 `matches()` 比对；新增/修改时加密 |
| [`controller/LoginController.java`](tlias-web-management/src/main/java/com/itheima/controller/LoginController.java) | 修改 | 日志不再打印整个 emp |
| [`sql/migrate-p0-4-password-bcrypt.sql`](sql/migrate-p0-4-password-bcrypt.sql) | 新增 | 表结构改造 + 存量明文密码批量替换 |

---

## 三、核心实现

### 3.1 依赖：只引 `spring-security-crypto`，不引整个 Spring Security

```xml
<!--密码加密: 只引 spring-security 的 crypto 模块(BCrypt), 不引入整个 Spring Security 框架-->
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-crypto</artifactId>
</dependency>
```

**为什么强调"只引 crypto"**：`spring-boot-starter-security` 一进来，框架会**默认给所有接口加上认证**（一个 HTTP Basic 弹窗 + 一串随机密码），整个项目的鉴权逻辑要重写。我们这里只想用它的 `BCryptPasswordEncoder`，不需要它的过滤器链，所以引单独的 `spring-security-crypto` —— 这个模块是独立的，没有任何框架副作用，也几乎不拉额外依赖。

版本不用写，由 `spring-boot-starter-parent` 统一管理（实测解析到 6.2.5）。

### 3.2 为什么是 BCrypt，不是 MD5/SHA256

| | MD5 / SHA256 | BCrypt |
|---|---|---|
| 可逆性 | 单向 | 单向 |
| **盐** | 默认没有，需要自己加 | **内置随机盐**，盐就存在密文里 |
| **速度** | 极快（GPU 每秒几十亿次） | **故意慢**（单次约 60ms） |
| 彩虹表 | 无盐时可秒破 | 无法使用 |

关键在第三行。MD5 的设计目标是"快"，而这恰恰是密码哈希最不想要的属性 —— 攻击者拿到密文后，用显卡每秒能试几十亿个候选密码。

BCrypt 反过来，**故意把单次计算变慢**（代价因子 cost，默认 10，即 2^10 轮）。60ms 对一次登录完全无感，但攻击者要枚举 10 亿个密码，就从"几秒"变成了"几十年"。

代价因子还能随硬件升级而调大，且**旧密文里的 cost 会被保留**，可以做到渐进升级（`upgradeEncoding()` + 登录时重新编码）。

密文格式：

```
$2a$10$4Gf9H5hkfr2rGnooSdA8F.r/Ys7YisOG1DMrzZYID8KjB9bilC9Ji
 ^^ ^^ ^^^^^^^^^^^^^^^^^^^^^^ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 版本 cost      22位随机盐                31位哈希
```

**盐是随机的，所以同一个密码每次加密结果都不同** —— 这一点直接决定了下面的登录改造方式。

### 3.3 登录改造：密码不能再当 SQL 查询条件

原来的写法：

```java
@Select("select * from emp where username = #{username} and password = #{password}")
Emp getByUsernameAndPassword(Emp emp);
```

这个写法**必须废弃**。因为 BCrypt 每次加密结果都不同，`where password = '123456'` 永远匹配不到库里的密文 —— 数据库里根本没有 `123456` 这个值。

改成：先按用户名把密文取出来，再在 Java 里比对。

```java
// EmpMapper
@Select("select * from emp where username = #{username}")
Emp getByUsername(String username);
```

```java
// EmpServiceImpl.login
//1. 参数校验: 密码为 null 时 BCrypt 的 matches 会抛异常(变成 500), 先挡掉
if(!StringUtils.hasText(emp.getUsername()) || !StringUtils.hasText(emp.getPassword())){
    return null;
}

//2. 按用户名查
Emp e = empMapper.getByUsername(emp.getUsername());

//3. 用户不存在时, 也空跑一次 BCrypt 比对再返回失败
if(e == null){
    passwordEncoder.matches(emp.getPassword(), DUMMY_PASSWORD);
    return null;
}

//4. 比对明文与密文(全程没有解密, BCrypt 不可逆)
if(!passwordEncoder.matches(emp.getPassword(), e.getPassword())){
    return null;
}
```

`matches(明文, 密文)` 的工作方式：从密文里把盐和 cost 取出来，用同样的参数对明文重新加密一次，然后比对两个哈希。全程不涉及任何"解密"。

**第 3 步为什么要在用户不存在时空跑一次？** 防止**时序攻击**。如果用户不存在就直接返回，这次请求会明显更快（少了一次 BCrypt 计算）；攻击者反复试就能靠响应时间差**枚举出系统里存在哪些用户名**。空跑一次让两条路径耗时一致。实测数据：

```
密码错误=0.063880   用户不存在=0.064129
密码错误=0.062187   用户不存在=0.062786
密码错误=0.064007   用户不存在=0.079926   ← 偶发抖动
密码错误=0.062607   用户不存在=0.062974
密码错误=0.062715   用户不存在=0.063119
```

差异落在网络抖动范围内，无法区分。

### 3.4 接口与日志脱敏：一个注解解决两处

```java
/**
 * 密码(数据库中存 BCrypt 密文)
 * WRITE_ONLY: 只允许反序列化(接收登录请求传来的明文密码), 序列化时一律不输出
 * ToString.Exclude: Lombok 生成的 toString 不含密码
 */
@JsonProperty(access = JsonProperty.Access.WRITE_ONLY)
@ToString.Exclude
private String password;
```

这两个注解分别堵住两条泄漏路径：

- **`@JsonProperty(WRITE_ONLY)`** —— Jackson 只在"反序列化"（接收登录请求的 JSON）时读它，"序列化"（返回响应）时永远不输出。所以 `/emps` 列表、`/emps/{id}` 详情都不会再带密码，但 `/login` 依然能正常收到前端传来的密码。

  > 为什么不用 `@JsonIgnore`：那个是**双向**屏蔽的，加上之后登录接口连密码都收不到，直接坏掉。`WRITE_ONLY` 才是"只进不出"。

- **`@ToString.Exclude`** —— Lombok 生成的 `toString()` 不再包含密码。这一条同时修好了两处日志：`LoginController` 的登录日志，以及操作日志切面 `Arrays.toString(args)` 落库的 `method_params`。**一个注解把问题 3 的两个分支一起解决了。**

### 3.5 初始密码：从"数据库默认值"改成"代码里显式加密"

原来的 `insert` 语句**根本没有 password 列**，新员工的密码是靠数据库列的 `DEFAULT '123456'` 兜底塞进去的 —— 存进去的自然是明文。

现在把这个默认值**从表结构里删掉**，改成代码显式设置：

```java
// EmpServiceImpl.save
//密码加密存储: 统一使用初始密码, 不采纳客户端传来的 password
emp.setPassword(passwordEncoder.encode(INIT_PASSWORD));
empMapper.insert(emp);
```

两个设计决定：

1. **不采纳客户端传来的 password**。这保持了原有行为（原来客户端传什么都会被数据库默认值覆盖），同时避免了一个常见漏洞 —— 攻击者通过"新增员工"接口的请求体里塞 `password` 字段，给自己设一个已知密码的账号。密码只能由系统按规则生成。
2. **去掉数据库默认值**。`DEFAULT '123456'` 这种"沉默的兜底"正是密码问题的温床：只要代码里忘了设密码，数据库就悄悄给你一个明文弱密码，而且**没有任何报错**。现在列改成了 `NOT NULL` 且无默认值，代码里漏设会**直接插入失败**，逼着你显式加密。

### 3.6 update 的防御性处理

`EmpMapper.xml` 的 update 里有一个分支：

```xml
<if test="password != null and password != ''">password = #{password},</if>
```

当前前端没有"修改密码"入口，不会传 password，所以这个分支是死代码。但万一将来有人随便传了个明文过来，就会被原样写进数据库。所以在 service 里先处理掉：

```java
String rawPassword = emp.getPassword();
emp.setPassword(StringUtils.hasText(rawPassword) ? passwordEncoder.encode(rawPassword) : null);
```

传了 → 加密后更新；没传 → 置 `null`，配合 XML 里的 `<if>` 跳过该列，原密码保持不变。

> 这里还有一个之前存在的**双重加密**隐患：如果前端"先查详情、再整体提交修改"（`queryInfoApi` → `updateEmpApi`，本项目前端就是这个模式），查出来的密文会被原样提交回去，然后被再加密一次，用户就登不上了。现在 `@JsonProperty(WRITE_ONLY)` 让详情接口不返回密文，这个隐患**顺带被消掉了** —— 前端拿不到 password，也就传不回来。

---

## 四、迁移脚本

[`sql/migrate-p0-4-password-bcrypt.sql`](sql/migrate-p0-4-password-bcrypt.sql)，分三步：

**第 1 步：改表结构**

```sql
ALTER TABLE emp
    MODIFY COLUMN password VARCHAR(100) NOT NULL COMMENT '密码, BCrypt 密文';
```

加宽到 100（而不是刚好 60），是为了将来换算法（如 Argon2 密文更长）时不用再改表。

**第 2 步：存量明文批量替换**

```sql
UPDATE emp SET password = '$2a$10$4Gf9H5hkfr2rGnooSdA8F.r/Ys7YisOG1DMrzZYID8KjB9bilC9Ji' WHERE password = '123456';
UPDATE emp SET password = '$2a$10$NC0ZsOvCSuLQloA0UqN.xuwUiafmz9RFE.MzkRxbiRgFqqPh9tYKK' WHERE password = '1234567';
UPDATE emp SET password = '$2a$10$TEgDp1ZoTtMdPwL1YNlN2emerCFbA5QAVve0WWln6W9/aiU220Df2' WHERE password = '12345678';
```

密文是用 `BCryptPasswordEncoder`（默认 cost=10）实际生成并逐一 `matches()` 验证过的。

**为什么是三条而不是一条 UPDATE**：库里有 3 种不同的明文密码。如果无脑把所有行刷成同一个哈希，密码本来是 `1234567` / `12345678` 的两个账号会被改成 `123456`，**直接登不上去**。这三条是照着 `group by password` 的实际结果写的：

```
mysql> select password, count(*) cnt from emp group by password;
+-----------+-----+
| 123456    |  28 |
| 1234567   |   1 |
| 12345678  |   1 |
+-----------+-----+
```

**第 3 步：校验**

```sql
SELECT id, username, password FROM emp WHERE password NOT LIKE '$2a$%';
```

必须返回 **0 行**。有结果就说明还存在非 BCrypt 格式的密码，这些账号会登不上去。

脚本可重复执行（第二次运行三步都不会产生变化）。

---

## 五、验证过程

### 5.1 漏跑迁移会怎样：不会静默放行 —— 实测

先**不跑迁移脚本**，直接启动改完代码的应用，用正确密码登录：

```
$ curl -X POST /login -d '{"username":"shinaian","password":"123456"}'
{"code":0,"msg":"用户名或密码错误","data":null}          ← 登录失败

后端日志:
WARN  o.s.security.crypto.bcrypt.BCryptPasswordEncoder - Encoded password does not look like BCrypt
```

这是**期望行为**：`matches()` 拿到一个不是 BCrypt 格式的密文（这里是旧的明文 `123456`）会返回 false 并打一条明确的 WARN。

**关键点是它"fail-closed"** —— 漏跑迁移的后果是"所有人登不上，日志里原因写得很清楚"，而不是"密码校验被绕过、谁都能登"。这是安全改造该有的失败方向。

### 5.2 迁移前 / 迁移后完整对比

| # | 验证项 | 迁移前 | 迁移后 |
|---|---|---|---|
| 1 | 正确密码 `123456` 登录 | ❌ 失败 | ✅ 成功 |
| 2 | 另一种密码 `12345678`（账号 liying） | ❌ 失败 | ✅ 成功 |
| 3 | 错误密码 | ❌ 失败 | ❌ 失败 |
| 4 | 不存在的用户名 | ❌ 失败 | ❌ 失败 |
| 5 | 请求体里不传 password | ❌ 失败 | ❌ 失败（不是 500） |

第 2 条特意验证了 `liying` —— 证明批量替换没有把不同密码的账号误刷成同一个。

### 5.3 接口泄漏修复 —— 改动前后实测对比

用 `git stash` 临时撤掉 `Emp.java` 的注解、重新编译启动，抓改动前的响应：

```
【改动前】
GET /emps?page=1&pageSize=1
{
  "id": 1,
  "username": "shinaian",
  "password": "$2a$10$WmCc2hLcNIeKCYDklCtj/.ATmevoQEIhd4y8JXrhksku0nDyvzFWm",   ← 密码被返回
  ...
}

$ curl ... "/emps?page=1&pageSize=10" | grep -o password | wc -l
10                                        ← 10 行数据, 10 个 password
```

```
【改动后】
$ curl ... "/emps?page=1&pageSize=10" | grep -o password | wc -l
0
```

> 注：截这张"改动前"的图时数据库已经迁移完了，所以显示的是密文。**迁移之前那一列是明文 `123456`，同样会被原样返回** —— 前面已经查过库里就是明文，接口又会把这一列整个输出，两条链合起来就是"明文密码经接口泄漏"。

### 5.4 日志脱敏 —— 实测

发一个带 password 的修改请求（密码值仍用 `123456`，不破坏数据），然后查操作日志：

```
【改动前的历史记录】
method_params: [Emp(id=52, username=..., password=, name=老邓, ...)]      ← 有 password 字段

【改动后】
method_params: [Emp(id=1, username=null, name=null, gender=null, phone=null,
                    job=null, salary=null, image=null, entryDate=null, deptId=null,
                    createTime=null, updateTime=2026-09-19T11:28:18.248445600,
                    deptName=null, exprList=null)]                        ← password 字段整个消失了
```

### 5.5 新增员工链路 —— 实测

```
$ curl -X POST /emps -d '{"username":"_p04_probe","name":"probe","gender":1,
                          "phone":"19999999999","job":1,"deptId":1}'
{"code":1,"msg":"success"}

$ mysql> select id, username, password from emp where username='_p04_probe';
+----+-------------+--------------------------------------------------------------+
| 55 | _p04_probe  | $2a$10$jDazDtCj3Fa5qZg0Q.P1UO1rEIBFJ9Uc4b8KFzivhYECp.qJ/X0NC |
+----+-------------+--------------------------------------------------------------+
                  ↑ 存的是密文, 不是 123456

$ curl -X POST /login -d '{"username":"_p04_probe","password":"123456"}'
{"code":1,"msg":"success", ...}          ← 用初始密码能正常登录
```

验证完已把这个探针账号删掉。

### 5.6 最终状态

```
$ mysql> select count(*) total, sum(password like '$2a$%') bcrypt from emp;
+-------+--------+
| total | bcrypt |
+-------+--------+
|    30 |     30 |
+-------+--------+
```

30 个账号，30 个都是 BCrypt 密文。

---

## 六、本次**没有**做的事（范围说明）

### 6.1 没有引入完整的 Spring Security

只用了它的 `crypto` 模块做密码哈希。**认证和鉴权仍然由项目自己的 `TokenFilter` + JWT 负责。**

要不要上完整的 Spring Security 是个独立决策：它能带来方法级鉴权（`@PreAuthorize`）、CSRF 防护、标准的过滤器链，但也要求把现有的鉴权逻辑整个重写。对这个项目来说收益不明显，属于过度改造。

### 6.2 没有做"强制首次登录修改密码"

真实系统里，用统一的初始密码 `123456` 发账号，是需要配合**首次登录强制改密**的，否则初始密码等于形同虚设（所有新员工的密码都一样，互相都能登）。

这个功能需要前端多一个改密页面 + 后端一个改密接口 + `emp` 表加个"是否已改密"的标记位，是独立的功能项。**面试时可以主动提这一点**，说明你知道初始密码的边界在哪。

### 6.3 没有做密码强度校验

现在可以把密码设成 `1`，也能通过。真实系统会有最小长度、复杂度、常见弱密码黑名单（如 `123456`、`password`）的校验。同理，属于独立功能项。

### 6.4 没有加"pepper"

BCrypt 的盐存在密文里，能防彩虹表；但如果攻击者**同时**拿到了数据库和代码，盐就保护不了什么了。进一步的做法是再加一层**全局密钥 pepper**（存在代码/环境变量里，不进数据库），哈希时混入：

```
hash = bcrypt(sha256(password + pepper))
```

这样即使数据库泄漏，没有 pepper 也破解不了。代价是 pepper 轮换会非常麻烦（所有密码都要重算）。本项目没做，但值得知道。

### 6.5 没有处理 `operate_log` 里的历史记录

历史记录里的明文密码是**改动之前**就落库的，这次的脱敏只对新记录生效。要清理旧数据得单独写 SQL。数据量小的话影响不大，但真实系统里这类"日志里的历史敏感数据"是需要专门清理的。

---

## 七、顺带发现

- **`operate_log.method_params` 是 `varchar(1000)`**，而 `Emp.toString()` 加上 `exprList` 后很容易超过 1000 字符，超长会被截断或报错。属于 P1-9（操作日志切面本身的问题）。
- **`Result` 类不是泛型**，`operate_log` 表也没有记录"操作结果"。这次改动没有涉及。

---

## 八、企业中密码是怎么存的（面试向）

这一节是本项目的做法和企业实践的对照。

### 8.1 算法选型

| 算法 | 现状 |
|---|---|
| MD5 / SHA1 / SHA256 | **禁止用于密码**。设计目标是快，GPU 每秒几十亿次，且无内置盐 |
| **BCrypt** | 目前最主流，成熟稳定，内置盐和代价因子。**本项目用的就是这个** |
| **Argon2** | 2015 年密码哈希竞赛冠军，抗 GPU / 抗 ASIC 更强（内存硬）。新系统首选，如 Argon2id |
| scrypt | 同样是内存硬，用得比 Argon2 少 |
| PBKDF2 | 老标准（FIPS 认证场景还在用），抗 GPU 能力弱于前两者 |

Spring Security 对这几个都提供了实现（`BCryptPasswordEncoder` / `Argon2PasswordEncoder` / `Pbkdf2PasswordEncoder` / `SCryptPasswordEncoder`），换算法只要换个 Bean 实现。**注意 Argon2 需要额外引 `bouncycastle` 依赖**。

### 8.2 为什么"慢"是特性

密码哈希和普通哈希的设计目标**恰好相反**：

- 普通哈希（文件校验、缓存 key）：越**快**越好
- 密码哈希：越**慢**越好

因为攻击场景不同。普通哈希的对手是"性能"，密码哈希的对手是"暴力枚举的人力/算力"。把单次计算从 1 微秒拉到 60 毫秒，正常登录毫无感觉，但攻击者的枚举速度直接掉 6 万倍。

**代价因子怎么选**：调到"你的服务器上单次校验约 100~250ms"这个量级。太低了不安全，太高了会成为 DoS 入口 —— 攻击者用大量登录请求就能把你的 CPU 打满（这也是为什么登录接口必须有**限流**）。

### 8.3 除了加密，企业还会做这些

1. **强制改密**：初始密码 / 重置后的密码，首次登录必须改
2. **密码策略**：最小长度（通常 ≥ 8 或 12）、复杂度、弱密码黑名单（比对 Have I Been Pwned 泄露库）
3. **登录限流 + 账号锁定**：连续失败 N 次锁定一段时间，防在线爆破
4. **撞库防护**：检测"同一密码在多个账号上被尝试"的模式
5. **pepper**：数据库之外的一道额外防线（见 6.4）
6. **审计**：记录"谁在什么时候改了密码"，但**绝不记录密码本身**
7. **密码历史**：不允许重用最近 N 次的密码

### 8.4 面试话术

> "密码用 BCrypt 存储，内置随机盐 + 可调代价因子；登录时用 `matches()` 比对，全程不解密。同时做了三件事：密码字段加了 `WRITE_ONLY` 保证接口不返回、加了 `ToString.Exclude` 保证不落日志、用户不存在时也空跑一次 BCrypt 比对防时序攻击枚举用户名。初始密码从数据库默认值改成代码里显式加密，因为数据库级的默认值会让'忘记设密码'变成一个没有报错的静默失败。"

> "如果继续往下做，我会补：强制首次改密、密码强度校验、登录限流防爆破。算法上如果要更强的抗 GPU 能力，会换成 Argon2id。"

---

## 九、下一步

改动停在分支 `feature/p0-4-password-bcrypt` 上，**没有合并到 main，也没有推送**。

至此"安全三连"（P0-2 配置外置 → P0-3 JWT 密钥 → P0-4 密码加密）做完。

按清单顺序，接下来是 **P0-5：精确白名单鉴权** —— `TokenFilter` 里用 `requestURI.contains("login")` 判断是否放行，这是个绕过漏洞（比如 `/emps/xxxlogin` 这样的路径也能命中）。同时要解决 Filter 和 Interceptor 两套鉴权实现并存的问题。
