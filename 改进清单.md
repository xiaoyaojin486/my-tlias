# tlias 智能学习辅助系统 — 改进清单

> 生成日期：2026-09-19
> 目的：把项目从「跟着教程敲完的作业」升级成「能扛住面试官追问的个人项目」
> 阅读方式：P0 是简历硬伤，上简历前建议一定处理；P1 是工程规范；P2 是加分项，挑性价比高的做

---

## 优先级概览

| 级别 | 数量 | 说明 | 建议投入 |
|---|---|---|---|
| **P0** | 8 项 | 面试官打开代码就会问 / 一追问就露馅 | 1~2 天 |
| **P1** | 11 项 | 代码规范和健壮性，体现"工程素养" | 2~3 天 |
| **P2** | 9 项 | 加技能点，让简历关键词更硬 | 按需挑 3~4 项 |

---

## P0 — 简历硬伤

### 1. 操作日志页面是"死页面"，后端根本没有查询接口 ⚠️ 最优先

**现状**：前端 [`src/api/log.js`](vue-tlias-management/src/api/log.js) 请求 `/log/page`，[`src/views/log/index.vue`](vue-tlias-management/src/views/log/index.vue) 也把它渲染成了表格页；但后端**没有任何 Controller 映射到 `/log`**，`OperateLogMapper` 只有 `insert` 方法。

也就是说：日志在往库里写，但页面上永远查不出来。面试官点一下这个菜单就发现了。

**改法**：
- `OperateLogMapper` 加 `@Select` 分页查询，`operate_log` 关联 `emp` 表带出 `operate_emp_name`（[`pojo/OperateLog.java:20`](tlias-web-management/src/main/java/com/itheima/pojo/OperateLog.java#L20) 已经预留了这个字段，用 PageHelper 分页即可）
- 新建 `LogController`，`@GetMapping("/log/page")` 返回 `Result<PageResult<OperateLog>>`

**收益**：一个完整可演示的「操作审计」模块，写进简历是个实打实的亮点。

---

### 2. 数据库密码明文写死在配置文件

**位置**：[`application.yml:7`](tlias-web-management/src/main/resources/application.yml#L7) `password: <本地数据库明文密码>`（原值已从本文档移除，避免文档本身泄漏）

**风险**：一旦推到 GitHub，密码就永久留在提交历史里（即使后来删掉也还在）。你的仓库是 public，这条必须先处理。

**改法**：
```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/tlias
    username: ${DB_USER:root}
    password: ${DB_PASSWORD}
```
本地开发再建一个 `application-local.yml`（写真实密码），加进 `.gitignore`。

---

### 3. JWT 签名密钥硬编码

**位置**：[`utils/JwtUtils.java:12`](tlias-web-management/src/main/java/com/itheima/utils/JwtUtils.java#L12)

```java
private static String signKey = "SVRIRUlNQQ==";  // 硬编码 + static
private static Long expire = 43200000L;
```

**两个问题**：一是密钥进仓库（同上，等于伪造 token 的钥匙公开了）；二是 `static` 字段没法注入配置，也不好写单元测试。

**改法**：抽成 `@ConfigurationProperties(prefix = "tlias.jwt")` 的配置类，`signKey` / `expire` 从 `application.yml` + 环境变量读；把 `JwtUtils` 从静态工具类改成 Spring Bean。

**顺带一提**：面试如果被问"你的 JWT 密钥是怎么管理的"，这题答好了很加分（可以延伸到密钥轮换、KMS）。

---

### 4. 密码明文存储 / 明文比对

**位置**：[`EmpServiceImpl.login()`](tlias-web-management/src/main/java/com/itheima/service/impl/EmpServiceImpl.java#L134) → `empMapper.getByUsernameAndPassword(emp)`

密码在数据库里是明文，登录靠 SQL 直接比对明文。这是简历项目里最容易被抓的安全问题。

**改法**：
- 引入 `spring-security-crypto`（只引这一个依赖，不用上全套 Spring Security，避免改造量爆炸）
- 存库用 `BCryptPasswordEncoder.encode()`，登录用 `matches()` 比对
- 顺手加个"新增员工时密码加密"的处理

---

### 5. 鉴权白名单靠字符串包含判断

**位置**：[`filter/TokenFilter.java:26`](tlias-web-management/src/main/java/com/itheima/filter/TokenFilter.java#L26)

```java
if (requestURI.contains("login")){   // 任何 URL 带 "login" 就放行
```

`/some-loginfo`、`/xxx/login-anything` 全都会被放行。应该用精确白名单：

```java
private static final Set<String> WHITE_LIST = Set.of("/login");
// 或者用 AntPathMatcher 匹配 /doc.html、/v3/api-docs/** 等
```

**同时注意**：项目里现在有 **两套鉴权代码**——[`TokenFilter`](tlias-web-management/src/main/java/com/itheima/filter/TokenFilter.java)（`@WebFilter`，实际生效）和 [`TokenInterceptor`](tlias-web-management/src/main/java/com/itheima/interceptor/TokenInterceptor.java)（[`WebConfig`](tlias-web-management/src/main/java/com/itheima/config/WebConfig.java) 里的注册代码全被注释了，实际没生效）。

面试官一定会问"你到底用的 Filter 还是 Interceptor，为什么"。**建议二选一删掉另一套**，并在 README 里写清楚选型理由（Filter 在前、能拿到原始请求；但 Interceptor 能拿到 HandlerMethod 和方法注解）。

---

### 6. 包名还是培训机构的

`com.itheima` —— 一眼就能看出是黑马教程项目。用 IDEA 的 `Refactor > Rename Package` 全局改成你自己的（比如 `com.huhaohua.tlias`），一次操作的事。

**顺便**：`logback.xml` 里日志路径写死了 `D:/tlias-*.log`，`static/upload.html` 也是教程遗留，一起清掉。

---

### 7. 一堆学习痕迹和死代码

上简历前建议清理这些：

| 文件 | 说明 |
|---|---|
| [`controller/SessionController.java`](tlias-web-management/src/main/java/com/itheima/controller/SessionController.java) | `/c1 /c2 /s1 /s2`，纯教学演示 Cookie/Session |
| [`filter/AbcFilter.java`](tlias-web-management/src/main/java/com/itheima/filter/AbcFilter.java)、[`filter/DemoFilter.java`](tlias-web-management/src/main/java/com/itheima/filter/DemoFilter.java) | 教学 demo |
| [`interceptor/DemoInterceptor.java`](tlias-web-management/src/main/java/com/itheima/interceptor/DemoInterceptor.java) | 同上 |
| [`src/test/`](tlias-web-management/src/test/java/com/itheima/) 下 7 个文件 | `Demo.java`、`ThreadLocalDemo.java`、`LogTest.java`、`LogTest2.java`、`UrlTest.java`、`UUIDTest.java` |
| [`EmpServiceImpl.java:45-46`](tlias-web-management/src/main/java/com/itheima/service/impl/EmpServiceImpl.java#L45-L46) | 注入了 `EmpLogService` **但从未调用**（员工变更日志功能半成品） |
| [`src/stores/counter.js`](vue-tlias-management/src/stores/counter.js) | Vite 脚手架自带的计数器 store，没用到 |
| [`DeptController.java:24-25`](tlias-web-management/src/main/java/com/itheima/controller/DeptController.java#L24-L25) | 注释掉的 `LoggerFactory` |

**决策点**：`EmpLogService` 是补全（做成"员工变更历史"）还是删掉？补全的话是多一个亮点，删掉的话要记得连 `EmpLogMapper` / `EmpLog` 一起清。

---

### 8. 项目"不可复现"——没有 README、没有建表 SQL、本地已经没有 git 仓库

**现状**：
- 后端**没有 README.md**（前端那个 README 还是 Vite 默认模板，一个字没改）
- **没有建表 SQL 脚本**（`tlias.sql`），别人拿到代码建不了库
- **没有 `.gitignore`**，本地 `.git` 目录也已经不存在了
- 依赖阿里云 OSS，但 README 里没说需要配哪两个环境变量（`OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET`，见 [`AliyunOSSOperator.java:32`](tlias-web-management/src/main/java/com/itheima/utils/AliyunOSSOperator.java#L32)）

**这是最容易被扣分的一项**：面试官想跑一下你的项目，结果跑不起来。

**改法**：
1. 导出建表语句 + 几条测试数据，放仓库根目录 `sql/tlias.sql`
2. 写 README：项目简介、技术栈、模块截图、**本地启动步骤**、环境变量清单、接口文档地址
3. 补 `.gitignore`（Java + Node + IDEA + `application-local.yml` + `.resume_work/`）
4. 重新 `git init`，按功能拆分提交（你的习惯是「一个功能一次提交 + `vX.Y-名称` 标签」，继续保持），最后推到 GitHub

---

## P1 — 工程规范与健壮性

### 9. 操作日志切面有 3 个隐患

**位置**：[`aspect/OperationLogAspect.java`](tlias-web-management/src/main/java/com/itheima/aspect/OperationLogAspect.java)

| 问题 | 说明 | 改法 |
|---|---|---|
| `result.toString()` 可能 NPE | 第 44 行。目标方法返回 `void` 时 `result` 为 `null` | 判空后再记录 |
| 参数直接入库，可能记录敏感信息 | 第 43 行 `Arrays.toString(args)`，登录/新增员工会把密码原样写进 `operate_log` 表 | 过滤 `HttpServletRequest`、`MultipartFile`、密码字段；或只记实体 ID |
| 方法抛异常时**完全不记录** | 第 30 行 `joinPoint.proceed()` 异常直接向上抛，日志丢失 —— 而"谁删数据失败了"恰恰是最需要审计的 | `try/catch` 后记录异常信息再重新抛出 |
| 同步写库拖慢接口 | 每条日志一次 DB 写入 | `@Async` + 线程池异步落库，或用 `REQUIRES_NEW` 事务隔离 |

**这是我扫下来觉得最值得改的一处**：改完之后"自定义注解 + AOP 实现无侵入操作日志（含耗时统计、异常记录、异步落库）"这句话才站得住。

---

### 10. 返回类型丢泛型

现在大量裸类型：`Result`、`PageResult`、`List<Map>`。IDEA 会标黄，面试官也会觉得别扭。

重点几处：
- [`StudentController.java:40-47`](tlias-web-management/src/main/java/com/itheima/controller/StudentController.java#L40-L47) — `Result page(...)`
- [`StudentServiceImpl.java:38`](tlias-web-management/src/main/java/com/itheima/service/impl/StudentServiceImpl.java#L38) — `PageResult page(...)`
- [`StudentServiceImpl.java:74`](tlias-web-management/src/main/java/com/itheima/service/impl/StudentServiceImpl.java#L74) — `List<Map> getStudentDegreeData()`
- [`EmpServiceImpl.java:113`](tlias-web-management/src/main/java/com/itheima/service/impl/EmpServiceImpl.java#L113) — `List<Map> jobDataList`

建议补 `Result<T>`、`PageResult<T>` 泛型，报表接口定义 `JobOption` 这类具体的 VO 而不是裸 `Map`。

---

### 11. 字段注入 → 构造器注入

`@Autowired` 字段注入遍布全项目，其中 [`EmpServiceImpl.java:45`](tlias-web-management/src/main/java/com/itheima/service/impl/EmpServiceImpl.java#L45) 甚至把依赖注入写在了两个方法中间。改成：

```java
@Service
@RequiredArgsConstructor
public class EmpServiceImpl implements EmpService {
    private final EmpMapper empMapper;   // final + 构造器注入
}
```

好处：依赖不可变、便于单测（不用起 Spring 容器）。这是个标准的"你懂不懂 Spring 最佳实践"信号。

---

### 12. 阿里云 OSS 工具类的问题

**位置**：[`utils/AliyunOSSOperator.java`](tlias-web-management/src/main/java/com/itheima/utils/AliyunOSSOperator.java)

- **每次调用都新建 OSSClient**（`upload`/`listFiles`/`deleteFile` 各建一次），内部是连接池，创建销毁开销大 → 应配成单例 `@Bean`
- [`listFiles()`](tlias-web-management/src/main/java/com/itheima/utils/AliyunOSSOperator.java#L83) 无文件时返回 `null` → 应返回空集合
- `listObjects` 单次最多返回 1000 条，**没有分页** → 文件多了清理任务会漏删，用 `listObjectsV2` + marker 循环
- [`FileCleanTask`](tlias-web-management/src/main/java/com/itheima/task/FileCleanTask.java) 里垃圾文件判定用 `List.contains`，文件多时是 O(n²) → 换 `HashSet`
- 同文件里 `MalformedURLException` 被包成 `RuntimeException` 抛出，**一个坏 URL 就中断整个清理任务** → 应 `filter` 掉非法 URL 继续执行

---

### 13. 全局异常处理器不够用

**位置**：[`exception/GlobalExceptionHandler.java`](tlias-web-management/src/main/java/com/itheima/exception/GlobalExceptionHandler.java)

- 兜底文案写死"对不起，您的网络有问题"，但实际是**服务端**异常 → 改成不误导的通用文案
- 没有处理 `MethodArgumentNotValidException`（@Valid 校验失败）、`MaxUploadSizeExceededException`（超过 10MB 上传限制）、`HttpMessageNotReadableException`
- 建议统一带 HTTP 状态码，而不是永远返回 200 + code

---

### 14. 没有参数校验

现在完全依赖前端校验，后端接口裸奔（比如 `/students/import/{clazzId}` 传个不存在的班级 ID 也没人拦）。加 `spring-boot-starter-validation`，在 DTO 上用 `@NotBlank` / `@Size` / `@NotNull` + `@Valid`。

---

### 15. 前端问题

**位置**：[`utils/request.js`](vue-tlias-management/src/utils/request.js)、[`router/index.js`](vue-tlias-management/src/router/index.js)

| 问题 | 说明 | 改法 |
|---|---|---|
| `console.log` 残留 | [`request.js:15`](vue-tlias-management/src/utils/request.js#L15) | 删 |
| 响应拦截器可能二次崩溃 | [`request.js:30`](vue-tlias-management/src/utils/request.js#L30) 直接读 `error.response.status`，网络错误/超时时 `error.response` 是 `undefined` | 先判空，区分超时/网络错误/401 |
| **路由没有守卫** | `router/index.js` 里没有 `beforeEach`，未登录直接访问 `/emp` 会先渲染页面再跳登录页 | 路由加 `meta.requiresAuth`，全局前置守卫校验 token |
| 登录态没进 Pinia | token 直接塞 `localStorage`，`stores/` 下只有一个脚手架 counter | 收敛到 Pinia + 持久化插件，统一管理用户信息/登出逻辑 |
| 登录表单体验 | 密码框回车不触发登录、按钮无 loading 态 | 加 `@keyup.enter` + `:loading` |
| 依赖偏旧 | `vite@3`、`vue@3.2`，当前生态是 vite 6 / vue 3.5 | 升级（注意 `@vitejs/plugin-vue` 要一起升） |

---

### 16. 其余零散项

- [`logback.xml`](tlias-web-management/src/main/resources/logback.xml) 日志路径写死 `D:/tlias-*.log` → 改成可配置的相对路径
- [`application.yml:16`](tlias-web-management/src/main/resources/application.yml#L16) `log-impl: StdOutImpl` 会把每条 SQL 打印到控制台，**生产环境是性能杀手** → 用 profile 区分环境
- [`mapper/StudentMapper.java`](tlias-web-management/src/main/java/com/itheima/mapper/StudentMapper.java) 用注解写 SQL，`EmpMapper`/`StudentMapper` 又混用 XML → 统一风格
- 后端没有 `.gitignore` / 没有 `.editorconfig`

---

## P2 — 加分项（按投入产出比排序）

> 目标是让简历上的技术关键词从「Spring Boot + MyBatis + Vue」扩到有辨识度的组合。**建议只挑 3~4 项做扎实**，铺太宽反而每项都答不深。

### ⭐ 17. 接口文档（性价比最高，半天）
引 `springdoc-openapi`（Spring Boot 3 用这个，别用老的 springfox）或 Knife4j，访问 `/doc.html` 就能看到全部接口。
**为什么值**：面试演示时掏出一个漂亮的在线接口文档，观感提升非常大；而且写文档的过程会逼你把接口设计再捋一遍。

### ⭐ 18. 单元测试（性价比最高，1 天）
现在 `src/test/` 下全是 demo，覆盖率为 0。建议优先覆盖：
- `EmpServiceImpl.save/update` 的**多表事务回滚**（员工 + 工作经历）
- `JwtUtils` 生成/解析/过期/篡改
- `TokenFilter` 的白名单、无 token、非法 token 三个分支
- `DeptServiceImpl.delete` 的"部门下有员工不能删"业务异常

**为什么值**：简历上"单元测试覆盖率 X%"是个硬指标；而且面试官问"你怎么保证代码质量"时，这是唯一有说服力的答案。

### 19. RBAC 权限控制
现在所有登录用户权限完全一样。加角色字段 + `@RequireRole` 自定义注解 + AOP 拦截（正好复用你已有的 AOP 经验），或上 Spring Security。
**为什么值**：权限设计是后端面试的必考题，做了就有具体的方案可以聊。

### 20. Redis
- 登录态存 Redis（支持主动登出、续期、异地登录互踢）—— 现在的 JWT 签发后无法失效
- 报表接口加 `@Cacheable` 缓存（报表是重 SQL，天然适合）

### 21. Docker 一键部署
后端 Dockerfile + `docker-compose.yml`（MySQL + Redis + 后端 + Nginx 托管前端静态资源）。
**为什么值**：简历写"熟悉 Docker 容器化部署"就有实证，而且面试官要跑你的项目时，一条 `docker compose up` 就能起来——直接解决 P0 第 8 项。

### 22. CI 流水线
GitHub Actions：push 触发 `mvn test` + `npm run build`，README 上挂个 build 通过徽章。

### 23. 登录接口限流
Redis + 令牌桶/IP 维度计数，防暴力破解。可以在登录失败 5 次后锁定，顺便让"密码加密"那条有地方施展。

### 24. 链路追踪 traceId
用 MDC 给每个请求生成 `traceId`，日志格式里带上，排查问题时能串起一次请求的所有日志。

### 25. 报表/接口优化
- 班级人数统计、员工职位分布这类 SQL 目前是实时聚合，数据量大时慢 → 加缓存或定时预聚合
- 可以把 PageHelper 换成 MyBatis-Plus（内置分页 + 条件构造器），减少手写动态 SQL —— 不过这属于"另起一套"，看你取舍

---

## 建议的动手顺序

```
第 1 步（半天）：P0-1 补全操作日志查询接口        ← 补上唯一一个"看得见的窟窿"
第 2 步（半天）：P0-2/3/4 配置外置 + 密码加密     ← 安全三连，面试必问
第 3 步（半天）：P0-5/6/7 清理死代码 + 换包名     ← 让代码"看起来像个人项目"
第 4 步（半天）：P0-8 补 SQL 脚本 + README + git  ← 让项目能被别人跑起来
第 5 步（1 天） ：P1-9 AOP 日志切面三个隐患      ← 把已有亮点真正打磨成亮点
第 6 步（1 天） ：P1-10~16 泛型/注入/OSS/异常/校验
第 7 步（挑 2~3 项）：P2 里的 ⭐ 接口文档 + 单元测试，再按精力挑 Docker 或 Redis
```

---

## 改完之后，简历上可以这样写（示例）

> 以下表述都是在**上面改动落地后**才站得住的，不要提前用。

- 基于 **Spring Boot 3 + MyBatis + MySQL** 开发教学管理后台，覆盖部门/员工/班级/学员/报表五大模块，后端 50+ 源文件，前端 **Vue 3 + Vite + Element Plus + Pinia** 共 8 个页面
- 基于 **JWT + Servlet Filter + ThreadLocal** 实现无状态登录鉴权，白名单精确匹配放行，统一 401 处理并联动前端路由守卫
- 基于**自定义注解 `@LogOperation` + Spring AOP 环绕通知**实现无侵入操作审计，记录操作人/类名/方法/参数/返回值/耗时，异步落库，并通过独立事务保证「业务失败但日志必留」
- 使用 `@Transactional(rollbackFor)` 保证员工基本信息与工作经历**多表写入的一致性**
- **EasyExcel** 实现学员信息批量导入/导出（含数据字典双向转换），配合 `@Scheduled` 定时任务清理阿里云 OSS 上的孤儿图片
- 全局异常处理 `@RestControllerAdvice` + 业务异常分层，统一 `Result<T>` 响应体
- **JUnit 5 + Mockito** 单元测试覆盖核心业务逻辑，行覆盖率 XX%（补齐后填）
- 接口文档 **SpringDoc / Knife4j**，Docker Compose 一键启动全套环境（补齐后填）
