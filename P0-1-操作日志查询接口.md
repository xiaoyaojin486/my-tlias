# P0-1 补全操作日志查询接口

> 分支：`feature/p0-1-operate-log-query`
> 完成日期：2026-09-19
> 对应改进清单：P0-1（简历硬伤第 1 条）

---

## 一、改之前是什么问题

前端的"日志管理"页面是**完整可用**的：[`src/views/log/index.vue`](vue-tlias-management/src/views/log/index.vue) 渲染了表格和分页器，[`src/api/log.js`](vue-tlias-management/src/api/log.js) 也在发请求：

```js
export const queryPageApi = (page, pageSize) => request.get(`/log/page?page=${page}&pageSize=${pageSize}`)
```

**但后端根本没有 `/log` 这个路由。** 全项目搜不到任何 Controller 映射到它，`OperateLogMapper` 只有 `insert` 一个方法。

结果是：操作日志一直在往数据库里写（`operate_log` 表有 19 条真实数据），但页面永远查不出来 —— 点开菜单就是一张空表。

这是简历项目里最尴尬的一类问题：**功能看起来做了，实际是死的**。面试官点一下菜单就露馅，而且会连带怀疑其他模块是不是也这样。

---

## 二、改动清单

| 文件 | 类型 | 说明 |
|---|---|---|
| [`mapper/OperateLogMapper.java`](tlias-web-management/src/main/java/com/itheima/mapper/OperateLogMapper.java) | 修改 | 新增 `selectPage()` 查询方法 |
| [`service/OperateLogService.java`](tlias-web-management/src/main/java/com/itheima/service/OperateLogService.java) | 新增 | Service 接口 |
| [`service/impl/OperateLogServiceImpl.java`](tlias-web-management/src/main/java/com/itheima/service/impl/OperateLogServiceImpl.java) | 新增 | Service 实现（PageHelper 分页） |
| [`controller/LogController.java`](tlias-web-management/src/main/java/com/itheima/controller/LogController.java) | 新增 | `GET /log/page` 接口 |

共 4 个文件，其中 3 个是新增。**没有改动任何已有逻辑**，是纯粹的"补洞"。

---

## 三、逐文件说明

### 1. OperateLogMapper — 查询 SQL

```java
@Select("select ol.*, e.name as operate_emp_name " +
        "from operate_log ol left join emp e on e.id = ol.operate_emp_id " +
        "order by ol.operate_time desc, ol.id desc")
List<OperateLog> selectPage();
```

### 2. OperateLogServiceImpl — 分页

```java
PageHelper.startPage(page, pageSize);
List<OperateLog> logList = operateLogMapper.selectPage();
Page<OperateLog> p = (Page<OperateLog>) logList;
return new PageResult<>(p.getTotal(), p.getResult());
```

### 3. LogController — 接口

```java
@GetMapping("/page")
public Result page(@RequestParam(defaultValue = "1") Integer page,
                   @RequestParam(defaultValue = "10") Integer pageSize){
    log.info("分页查询操作日志, page: {}, pageSize: {}", page, pageSize);
    PageResult<OperateLog> pageResult = operateLogService.page(page, pageSize);
    return Result.success(pageResult);
}
```

---

## 四、三个关键设计决策

### 决策 1：为什么必须 JOIN `emp` 表

我先查了实际表结构，发现 `operate_log` 表**没有 `operate_emp_name` 这一列**：

```
`operate_emp_id` int unsigned  COMMENT '操作人ID'
```

而前端页面第一列要显示的偏偏就是 `operateEmpName`（操作人姓名）：

```html
<el-table-column prop="operateEmpName" label="操作人" align="center" width="80px"/>
```

所以只能通过 `operate_emp_id` 关联 `emp` 表取 `name`。POJO 里 [`OperateLog.java:20`](tlias-web-management/src/main/java/com/itheima/pojo/OperateLog.java#L20) 其实早就预留了这个字段，注释写着"页面展示字段" —— 说明设计意图就是 JOIN 出来的，只是当时没写查询。

### 决策 2：为什么用 `left join` 而不是 `inner join`

这是个**审计日志**功能。如果用 `inner join`，一旦某个操作人从 `emp` 表被删除，他历史上所有的操作记录都会**从日志页面凭空消失** —— 审计日志最不能容忍的就是"记录会丢"。

用 `left join`，操作人被删后日志仍在，只是 `operateEmpName` 显示为 `null`。审计场景下，"有一条记录但我不知道是谁" 远好过 "记录不见了"。

### 决策 3：为什么要二级排序 `order by operate_time desc, ol.id desc`

只按 `operate_time desc` 排是不够的。`operate_time` 是 `datetime`（秒级精度，见建表语句），**同一秒内发生的多条操作时间完全相同**。时间相同时 MySQL 不保证返回顺序稳定 —— 这会导致分页出现经典的"翻页时同一条记录出现两次、另一条却看不到"。

加上 `ol.id desc` 作为决胜字段，顺序就唯一确定了，分页结果稳定。

---

## 五、验证过程

不是"编译通过就算完"，我实际起了服务打了接口。

**验证环境**：Java 21 / Maven 3.6.3 / MySQL 8.0（3306），数据库 `tlias` 中 `operate_log` 表有 19 条真实数据。

**验证步骤与结果**：

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 应用启动 | ✅ `Started TliasWebManagementApplication in 2.154 seconds` |
| 2 | `/log/page?page=1&pageSize=5` | ✅ `code:1`，`total:19`，本页 5 条 |
| 3 | `/log/page?page=2&pageSize=15` | ✅ `total:19`，本页 4 条（19-15=4，分页正确） |
| 4 | `operateEmpName` 是否真的 JOIN 出来了 | ✅ 返回"李逵"、"宋江"等真实姓名 |
| 5 | 未携带 token 访问 | ✅ 401（鉴权正常，接口没有绕过过滤器） |

**实际响应数据**（节选）：

```
operateEmpName   costTime   className            method   params
--------------------------------------------------------------------------------
李逵              12         EmpController        save     [Emp(id=52, username=dengden
宋江              45         EmpController        update   [Emp(id=30, username=liyun,
李逵              5          DeptController       delete   [26]
李逵              4          DeptController       update   [Dept(id=29, name=后勤部...
李逵              6          DeptController       update   [Dept(id=31, name=研发部...

字段完整性检查：
  id               = 19
  operateEmpId     = 8
  operateTime      = 2024-08-26T16:41:25
  className        = com.itheima.controller.EmpController
  methodName       = save
  methodParams     = [Emp(id=52, username=dengdengdengdcneg, password=, name=老邓, ...
  returnValue      = Result(code=1, msg=success, data=null)
  costTime         = 12
  operateEmpName   = 李逵      ← JOIN 出来了
```

前端需要的 `data.total` 和 `data.rows` 两个字段都对得上，页面不用改一行代码。

---

## 六、本次**没有**做的事（范围说明）

为了保持"一次只改一件事"，以下几项我**看见了但没动**，留到对应清单项处理：

1. **没有泛型化 `Result`**。`Result` 是非泛型类（内部是 `Object data`），泛型化会波及全部 8 个 Controller，属于 P1-10，不适合混在这次改动里。新写的 Controller 与现有代码保持了一致的 `Result` 用法。
2. **没有清理日志表里的敏感数据**。见下节，属于 P1-9。
3. **没有加分页之外的筛选条件**（按操作人 / 时间范围 / 方法名过滤）。前端目前也没有对应的筛选表单，后端单方面加接口没人用。如果要做，前后端得一起改 —— 可以作为独立一项。
4. **前端 `log/index.vue` 一行没改**。

---

## 七、顺带发现的问题（喂给后续清单）

### ⚠️ 日志表确实在明文记录密码 —— 这次拿到了实证

验证时返回的这条数据：

```
methodParams = [Emp(id=52, username=dengdengdengdcneg, password=, name=老邓, gender=2, ...
```

`methodParams` 是 `Arrays.toString(args)` 的结果，也就是**直接序列化了整个 `Emp` 实体，包含 `password` 字段**。这条恰好密码为空，但只要用户填了密码，它就会**以明文形式永久写进 `operate_log` 表**。

这条日志恰好是 `/emps` 新增员工接口产生的 —— 而新增员工时前端一定会让用户填密码。

这是 P1-9（操作日志切面隐患）的**第一条**，之前只是从代码推断，现在是实测证据。

### 其他

- `returnValue` 字段存的是 `Result(code=1, msg=success, data=null)`。对于返回列表的接口（比如员工分页查询），这里会存下**整个结果集**，字段长度上限是 `varchar(2000)` —— 数据量大时会被截断（或者插入报错）。也归在 P1-9。
- 前端的 `scope.row.methodParams.length`（[`log/index.vue:59`](vue-tlias-management/src/views/log/index.vue#L59)）没有判空。当前数据里 `methodParams` 都有值，暂时不会崩，但属于潜在隐患。

---

## 八、下一步

本次改动停在分支上，**没有合并到 main，也没有推送**，等你看过之后再决定。

建议紧接着做 P0-2 / P0-3 / P0-4（配置外置、JWT 密钥、密码加密）—— 这三项都是"安全三连"，改动量小但面试必问。
